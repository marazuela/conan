# Maintenance Session — 2026-04-14 22:01 UTC cycle

**Lock acquired**: 2026-04-14T22:01:00Z (prior UNLOCKED, last held maint-2026-04-14 16:58 cycle)
**Session ID**: maintenance-2026-04-14 (22:01)

---

## PHASE 1 — Orientation

- SESSION_LOCK.md was UNLOCKED (Released 2026-04-14T17:02:44Z by maint-2026-04-14 16:58).
- Acquired lock cleanly.
- Dependencies installed (requests, bs4, lxml, yfinance, openpyxl, pandas) — OK.
- Read SESSION_STATE.md (S56 operational) + INSTRUCTIONS.md.
- Last operational session S56 at 2026-04-14 16:55 UTC captured 2 live M&A deals (AVNS + GSAT). Warnings section item 1 notes "file-truncation bug remains latent risk — S55 reported 3 broken, S56 reported 0 broken. Intermittent regression."

---

## PHASE 2 — Structural Health

### 2a. Compile-check (CRITICAL FINDING)

**FOUND BROKEN FILE**: `tools/convergence_engine.py` failed py_compile with:
```
File "tools/convergence_engine.py", line 560
    min_strategies=args.min_strategies,
IndentationError: unexpected indent
```

**Diagnosis**: File had 569 lines. Lines 1-559 ended correctly at `main()`. Lines 560-569 were an orphaned tail fragment — an 10-line duplication of code from the `run_convergence(...)` call and subsequent `generate_report` block that already existed earlier in `main()` (lines 548-555). Classic file-truncation / duplicated-tail pattern matching the S46/S55 regression profile flagged in SESSION_STATE warning #1.

**Fix**: Truncated file to 559 lines via `head -n 559`. Verified:
- `py_compile` now clean.
- `tail -5` shows proper termination: blank, blank, `if __name__ == "__main__":`, `    main()`.
- `skip_keywords` tuple still contains `"convergence"` at line 160-161 (prevents feedback loop) — unchanged.
- Updated version header line 2: `v1.2 — 2026-04-13` → `v1.3 — 2026-04-14 maint: removed 10-line orphaned tail fragment causing IndentationError at line 560`.

**Final sweep**: 14/14 tools compile CLEAN.

| Tool | Status |
|------|--------|
| companies_house_monitor.py | OK |
| congressional_trading.py | OK |
| contract_monitor.py | OK |
| convergence_engine.py | **FIXED** |
| edgar_filing_monitor.py | OK |
| esma_short_scanner.py | OK |
| fda_pdufa_pipeline.py | OK |
| google_trends_scanner.py | OK |
| mcap_cache.py | OK |
| openfigi_resolver.py | OK |
| pipeline_runner.py | OK |
| run_post_scan.py | OK |
| run_scanner.py | OK |
| uk_gazette_insolvency_scanner.py | OK |

### 2b. __pycache__ cleanup
- `rm -rf tools/__pycache__/` → "Operation not permitted" (sandbox restriction — same as S56 reported in warning #18).
- Mitigation: Force-recompiled `convergence_engine.py` directly via `py_compile.compile(...)` to overwrite the stale .pyc in place. This should ensure the operational scanner picks up the fixed source.
- Risk: If sandbox FS semantics block .pyc overwrite as well as delete, the operational task might still load stale bytecode. This is the same risk flagged by S56 maintenance; the file edit itself will invalidate the .pyc via mtime mismatch in standard CPython loading, so this should not be an issue in practice — but worth flagging.

### 2c. API reachability (7/7 GREEN — best reading since S56)

| Endpoint | Status |
|----------|--------|
| SEC EFTS `efts.sec.gov/LATEST/search-index` | 200 |
| Capitol Trades `/trades?page=1` | 200 (table element present) |
| USAspending POST `/v2/search/spending_by_award/` | 200 |
| ClinicalTrials.gov `/api/v2/studies` | 200 |
| openFDA `/drug/drugsfda.json` | 200 |
| FCA UK XLSX download | 200 |
| OpenFIGI POST `/v3/mapping` | 200 |

### 2d. Signals directory health
- 156 JSON files.
- Largest JSONs are legitimate `esma_short_*` (up to 1.58 MB, full EU short-position universe) and `congressional_*` (up to 125 KB, daily tx volume). **No convergence_*.json files >3 KB** — feedback-loop corruption absent. `skip_keywords` contains `"convergence"` confirmed.
- Dedup pruning:
  - `congressional_dedup.json`: 603 entries, cutoff 2026-03-31, 0 pruned (all within 14-day window — healthy, recent operational activity).
  - `contract_dedup.json`: 8 entries, cutoff 2026-03-15, 0 pruned.

### 2e. Lock state (already covered above)

---

## PHASE 3 — Signal Quality

### 3a. pdufa_watchlist.json (42 entries)
- No past PDUFA dates still marked "active". Clean.
- `XSPRAY.ST` and `6446.TW` (non-US suffixes) correctly carry `status: "non_tradeable"`.
- Ticker-drug duplicates: MNKD (Afrezza + FUROSCIX), IONS (Olezarsen + Zilganersen), RARE (DTX401 + UX111) — all legitimate (different drugs, same sponsor).
- Status distribution: active=32, approved=2, resolved_crl=1, linked_to_*=2, non_tradeable=5. Total 42 ✓.

### 3b. edgar_rotation_state.json
All 4 categories scanned within last 24h (as of 2026-04-14 16:09 UTC):
- activist: 10:10 UTC
- mna: 16:09 UTC (last)
- distress: 06:11 UTC
- governance: 09:09 UTC

Next rotation index = 1, last = "mna". Next operational scan should cycle to the next category per rotation sequence (likely `bankruptcy` per SESSION_STATE note, or `distress` — depends on pipeline_runner sequence). Rotation healthy.

### 3c. Orphaned signal files
`_scanner_result_*.json` files present but all timestamps within last ~6h. No stale orphans requiring attention.

---

## PHASE 4 — Bug Detection

### 4b. Silent error swallowing (informational — no mass fixes per scope rule)
19 occurrences of `except ...: pass` across 10 tools. Most are intentional (per-row parse tolerance, optional cache misses, best-effort OS ops). Notable ones that might warrant log.warning upgrades (future maintenance):
- `pipeline_runner.py:220` OSError pass — could hide FS issues
- `run_post_scan.py:304, 352` broad Exception/OSError pass
- `mcap_cache.py:37` Exception pass — cache miss, likely OK

**Not modified this session** — out of scope for a maintenance pass without a concrete failure to pin on any of these.

### 4c. Version bump
Only `convergence_engine.py` modified. Header bumped v1.2 → v1.3 with rationale.

---

## Fixes delivered this session

1. **tools/convergence_engine.py** — Removed 10-line orphaned tail fragment (lines 560-569) that caused `IndentationError: unexpected indent` at line 560. Version header bumped v1.2 → v1.3. Force-recompiled to overwrite stale .pyc. **This would have broken the operational scanner's convergence phase (S57) had it run before this maintenance cycle.**

---

## Open items / notes for next operational session (S57)

- **convergence_engine.py was broken on arrival** — file-truncation bug from warning #1 struck again this cycle. S56 saw clean file; between S56 shutdown (≈17:02 UTC) and this maintenance run (22:01 UTC), the file lost integrity. No operational scanner ran in that window (next cron at 18:00 UTC operational was actually the 16:00 cycle per SESSION_STATE; 19:00 UTC cycle presumably aborted or the bug arose after). Root cause for the truncation/duplication remains unidentified — it appears to be an IDE/sync artifact rather than a script bug.
- SESSION_STATE warning #1 remains active and was triggered again. Consider adding a pre-shutdown py_compile validation sweep to operational sessions (priority 5 in queue).
- 14/14 py_compile CLEAN post-fix, 7/7 APIs GREEN — system ready for next operational cycle.
